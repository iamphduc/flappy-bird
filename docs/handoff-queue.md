# Handoff Queue

**The human is the ultimate arbiter** — `BLOCKED` entries halt the orchestrator until acknowledged. **One line per entry.**

Format: `` - `[YYYY-MM-DD · TYPE · from → to · sprint: <slug> · slice: <code>]` <body> **Resolution:** pending `` (or `**Resolution:** <YYYY-MM-DD> — <what changed> [optional link to docs/decisions.md#anchor]`).

Entries are **date-keyed and append-only** (newest at the tail) — reference one by its `[date · from → to]` header plus a few words of its body, never by position (a same-day header can repeat; the body disambiguates). `from`/`to` is a **role**, not an agent name — `engineer` / `reviewer` / `orchestrator` / `sprint-planner` / `planner` / `human` — so it stays stable when an agent is renamed. Omit `slice:` for sprint-wide entries, `sprint:` for project-wide ones.

Types: `BLOCKED` halts · `PENDING` defers · `SOLVED` informational, only emitted alongside a `BLOCKED` or `PENDING` to mark a related thing resolved inline.

Resolve inline (do not delete prematurely); if decision-worthy, write a one-liner to `docs/decisions.md` and link from the Resolution line. At sprint end the orchestrator drops the oldest **resolved** entries beyond 100 (by date) — no renumbering; unresolved entries (`Resolution: pending`) are never pruned.

---
- `[2026-09-24 · BLOCKED · orchestrator → human]` autopilot preflight halted (gate 3, auto-merge-fail): no CI on pull requests (brief `## CI` is `none`, no `.github/workflows/`); also no `origin` remote, no commits on `main`, and `## Smoke recipe` unfilled — plan flappy-efficiency-mvp cannot start. **Resolution:** 2026-09-24 — GitHub remote added, bootstrap PR #1 merged (skeleton, smoke recipe, pod-ci on pull_request); autopilot resumed
- `[2026-09-24 · PENDING · engineer → human · sprint: game-core · slice: C1]` `actionFromPointer` maps unknown `pointerType` (e.g. `''`) to `click` rather than `null`; covered by its own test. **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: game-core · slice: C1]` browser check limited to `wait "Server OK"` — chrome-devtools MCP `new_page` locked ("browser already running"), axi `snapshot` fails with pageId error; no DOM/console/network check. **Resolution:** 2026-09-24 — the blocking Chrome was stopped with the human's OK; the MCP browser has worked for every slice and wave check since game-core C2
- `[2026-09-24 · PENDING · engineer → human · sprint: game-core · slice: E1]` regression flap list locked after engine existed (test-only commit `4b6e968` before impl): seed 42, flaps `[13,52,90,128,166,199,237]` → score 2, death step 296, cause `ground`; C2 uses it for its scripted browser check. Tuning constants will change these values. **Resolution:** 2026-09-24 — handled in sprint recording: used as the recorded scripted smoke check in docs/codebase-structure.md
- `[2026-09-24 · PENDING · engineer → human · sprint: game-core · slice: E1]` engine defaults chosen: `y` = top of square hitbox; flap at step n applies n→n+1 and replaces gravity that step; pipe check before ground check; pipe passed when `x + PIPE_WIDTH < BIRD_X`; `flapCount` counts only flaps before death. **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: game-core · slice: C2]` scripted session: a flap in `over` restarts with the SAME seed (flap list only fits that seed); unscripted restarts use a new random seed. Scripted runs ignore player flaps in ready/playing, but a flap in `over` still restarts and pause still works. **Resolution:** 2026-09-24 — handled in sprint recording: kept; C2 reserves scripted games with their own dev seed, and a restart re-records the same seed
- `[2026-09-24 · PENDING · engineer → human · sprint: game-core · slice: C2]` several player flaps in one frame collapse into one pending flap for the next step; pausing clears it — revisit if the recording sprint needs every raw press. **Resolution:** 2026-09-24 — handled in sprint recording: every press recorded as its own flap event, server stores pressCount and flapCount separately ([decisions](decisions.md))
- `[2026-09-24 · PENDING · engineer → human · sprint: game-core · slice: C2]` `/favicon.ico` 404 is the only console error (pre-existing); touch and 30 Hz were emulated via CDP, not tested on real hardware. **Resolution:** 2026-09-24 — handled in sprint recording: inline empty favicon added (C2); touch and 30 Hz still emulated only
- `[2026-09-24 · PENDING · engineer → human · sprint: recording · slice: P1]` added `MAX_ID_LENGTH = 64` (ids must be 1–64 chars; empty ids rejected) beyond the spec; `ReplayEventsResult` = engine `ReplayResult` + `pressCount` (includes `finalState`); `parseClientMessage` validates events individually — seq contiguity/uniqueness is left to S2 ingest. **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: recording · slice: S1]` two extra `app.test.ts` DB-close tests landed in impl commit `1ffc8d8`, not the test commit (checked red by disabling `db.close()`); `node:sqlite` prints a harmless `ExperimentalWarning` once per process on Node 22 (tests pass on 22.20 and 24.16, no flag needed); smoke recipe "DB setup" is C2's to update. **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: recording · slice: C1]` for C2 wiring: `checkPlayer` throws when the server can't answer (null only on 404) so an offline start keeps the stored player — catch it; `registerPlayer`/`reserveGame` return null on failure; forward `session.events` above the last sent seq (recorder ignores dup/acked seqs anyway); a scripted session records `start` on its first tick, so C2 must hold the scripted start until the game is assigned. **Resolution:** 2026-09-24 — handled in sprint recording: C2 wired all of these and checked them in the browser
- `[2026-09-24 · PENDING · engineer → human · sprint: recording · slice: C1]` `Session.pendingFlap` replaced by `pendingPresses: RecordedFlapSource[]`; recorder sends events right after `hello` without waiting for `welcome` (relayed to S2: must process one socket's messages in order); extra typecheck test `cb1f23f` makes `onmessage` take `(ev: never) => void` so a browser `WebSocket` fits `SocketLike`. **Resolution:** pending
- `[2026-09-24 · SOLVED · engineer → human · sprint: recording · slice: S2]` C1's hello-then-events-without-waiting: the ws handler is fully synchronous (`DatabaseSync`), so one socket's messages run strictly in order; pinned by a test. **Resolution:** 2026-09-24 — handled in S2
- `[2026-09-24 · PENDING · engineer → human · sprint: recording · slice: S2]` server replay cap: maxSteps = min(max(claimedDeathStep+1, DEFAULT_MAX_STEPS), 1 h); a bird still alive at the cap is stored `complete` with null death + `mismatch = 1` and gets no `result` message — the stats sprint should decide how to treat these. **Resolution:** 2026-09-24 — handled in sprint stats: such games get 409 `no-server-result`, no summary and no trend point; other mismatch games get a summary with `countsInStats: false` ([decisions](decisions.md))
- `[2026-09-24 · PENDING · engineer → human · sprint: recording · slice: S2]` ingest rules beyond spec: batch with any seq past the first death is rejected whole (`after-death`); resent seq with different content silently ignored (first write wins); events for a `complete` game are acked and skipped; extra error codes `after-death`/`server-error`; 1 MiB frame limit (close 1009); sweeper survives failed sweeps, timer `unref`'d; two own tests fixed in separate commits (`d1545b3`, `d05f6c6`), no assertion weakened; no `@types/ws`. **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: recording · slice: C2]` no red→green test commit: all C2 criteria are `[manual]` DOM wiring in `main.ts` (checked in the browser); with the API down the console shows Vite-proxy `500`s for `/api/*` plus WS connect errors; extras: `(reconnecting)` / `Enter a nickname…` status text and a failed reservation retries when the recorder comes back online. **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: stats · slice: C1]` `trendChartSvg` y labels are always the data min/max (equal values → both labels overlap and read the same); colors are `currentColor` for text/axes and fixed `#2a7fb8` for the line — C3 may restyle. **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: stats · slice: S1]` locked seed-42 summary (flaps `[13,52,90,128,166,199,237]`) for C2/C3: score 2, deathStep 296 `ground`, durationMs 4933.33, flaps 7, presses 7, extraPresses 0, scorePerFlap 0.2857, wastedFlaps 0 (0 rising, 0 overshoot), flapGapMs avg 622.22 / shortest 550 / longest 650, countsInStats true → line `Wasted flaps: 0 of 7 (0 while rising, 0 too high)`. Overshoot fixture: seed 42 flaps `[13,40]`; `[13,16,40]` = 1 rising + 1 overshoot. **Resolution:** 2026-09-24 — written into the smoke recipe's Summary/Stats checks by C3; orchestrator followed both on the stats wave-2 head and they matched
- `[2026-09-24 · PENDING · engineer → human · sprint: stats · slice: S1]` test commit `6bb2dbe` also carries the type scaffold and throwing stubs (so red failed on behavior, not imports); `playedAt` falls back to `createdAt` when `lastEventAt` is null (never for complete games). **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: stats · slice: C2]` `My stats` link → `/stats.html` could only be checked on the merged wave (page is C3's); test commit `503985e` carries a throwing stub `summary.ts`; `main.ts` panel wiring is `[manual]`-only (checked in browser); the `Could not load the summary` state is covered by unit tests only, not in the browser. **Resolution:** pending
- `[2026-09-24 · PENDING · engineer → human · sprint: stats · slice: C3]` Summary-check smoke step written from the sprint doc (orchestrator verifies on the wave head); a player whose games all have 0 flaps sees table rows but both charts say "No complete games yet"; `fetchStats` maps a 200 with no body to `error`; brief/known-issue say `pnpm dev` stops both apps when one dies, but killing only the API left Vite up (docs not changed). **Resolution:** pending
